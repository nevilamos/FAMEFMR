% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numericInputModule.R
\name{numericInputModuleUI}
\alias{numericInputModuleUI}
\title{Validated Numeric Input Module - UI}
\usage{
numericInputModuleUI(
  id,
  integerOnly = TRUE,
  label = NULL,
  minVal = NULL,
  maxVal = NULL,
  defaultVal = NA
)
}
\arguments{
\item{id}{Module ID.}

\item{integerOnly}{Logical. If TRUE (default), label and validation refer to "integer"; otherwise "number".}

\item{label}{Optional label text to display. If NULL, label is automatically generated using minVal and maxVal.}

\item{minVal}{Optional minimum allowed value (used for display and validation).}

\item{maxVal}{Optional maximum allowed value (used for display and validation).}

\item{defaultVal}{Optional default value (used as starting value in the input, defaults to NA).}
}
\value{
Shiny UI element for the numeric input.
}
\description{
Provides a numeric input that validates user input on Enter or blur.
Accepts NA (if allowed) or numeric/integer values within optional bounds.
Invalid input triggers a small popup modal and resets to the last valid value.
}
\details{
Label and validation messages automatically adapt based on \code{integerOnly}, \code{minVal}, and \code{maxVal}.
}
\examples{
\dontrun{
library(shiny)
ui <- fluidPage(
  h3("Validated Numeric Input Examples"),
  fluidRow(
    column(6, numericInputModuleUI("n1", integerOnly = TRUE, minVal = 0, maxVal = 100, defaultVal = 10)),
    column(6, numericInputModuleUI("n2", integerOnly = FALSE, defaultVal = 3.14))
  ),
  fluidRow(
    column(6, numericInputModuleUI("n3", integerOnly = TRUE, minVal = 10)),
    column(6, numericInputModuleUI("n4", integerOnly = TRUE, maxVal = 50))
  ),
  hr(),
  verbatimTextOutput("values")
)

server <- function(input, output, session) {
  v1 <- numericInputModuleServer("n1")
  v2 <- numericInputModuleServer("n2")
  v3 <- numericInputModuleServer("n3")
  v4 <- numericInputModuleServer("n4")

  output$values <- renderPrint({
    list(n1 = v1(), n2 = v2(), n3 = v3(), n4 = v4())
  })
}

shinyApp(ui, server)
}
}
