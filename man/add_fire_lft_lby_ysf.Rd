% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_fire_lft_lby_ysf.R
\name{add_fire_lft_lby_ysf}
\alias{add_fire_lft_lby_ysf}
\title{Add interval, YSF/LBY, and last fire type (LFT) columns to an output data frame}
\usage{
add_fire_lft_lby_ysf(
  OutDF,
  v,
  max_interval = 0L,
  start.SEASON = NA_integer_,
  end.SEASON = NA_integer_,
  seas_pattern = "SEAS",
  firetype_pattern = "FireType",
  verbose = TRUE
)
}
\arguments{
\item{OutDF}{data.frame containing SEAS* and FireType* columns}

\item{v}{terra vector fire history containing SEASON field (calendar years)}

\item{max_interval}{integer; if > 0 calls fireTypeLowToHigh(); if 0 skips; if < 0 errors}

\item{start.SEASON}{optional integer start year; if NA uses 2nd smallest unique v$SEASON (or smallest if only one)}

\item{end.SEASON}{optional integer end year; if NA uses max(v$SEASON); otherwise max(end.SEASON, max(v$SEASON))}

\item{seas_pattern}{regex/pattern used to find season columns in OutDF (default "SEAS")}

\item{firetype_pattern}{regex/pattern used to find fire type columns in OutDF (default "FireType")}

\item{verbose}{logical; print progress messages}

\item{LBY_f}{function(M, y) returning last burnt year (calendar year) per row; NA if none}

\item{fireTypeLowToHigh}{optional function(max_interval, Interval_Matrix, Firetype_Matrix) returning updated fire types}
}
\value{
Updated OutDF with INT*, YSF*, LBY*, LFT* columns appended
}
\description{
Wraps the provided workflow:
\itemize{
\item Identifies SEAS* and FireType* columns in OutDF
\item Computes interval columns INT01..INTxx from SEAS years (0 treated as missing)
\item Optionally updates FireType matrix using fireTypeLowToHigh() when max_interval > 0
\item Computes LBY (last burnt year), YSF (years since fire), and LFT (last fire type) for each year
in a calendar-year TimeSpan and appends them to OutDF.
}

Assumptions:
\itemize{
\item SEAS values are calendar years, with 0 meaning "no event"
\item LBY_f(M, y) returns NA when no previous fire exists for year y (as in your implementation)
}
}
