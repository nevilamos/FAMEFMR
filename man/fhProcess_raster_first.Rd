% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fhProcess_raster_first.R
\name{fhProcess_raster_first}
\alias{fhProcess_raster_first}
\title{Fire history raster workflow (wrapper): rasterize -> pack -> rebuild -> analyse}
\usage{
fhProcess_raster_first(
  r_template = template_r,
  vector_file = infile,
  fields = c("SEASON", "FIRETYPE_NO"),
  combField_multiplier = 10L,
  background = 0,
  overwrite = TRUE,
  datatype = "INT2U",
  compress = "LZW",
  memfrac = 0.8,
  progress = TRUE,
  OtherAndUnknown = 2,
  start.SEASON = 1980,
  end.SEASON = NA,
  max_interval = 0
)
}
\arguments{
\item{r_template}{A \code{SpatRaster} template defining target extent/resolution/CRS.
Defaults to \code{template_r}.}

\item{vector_file}{Path to a vector file readable by \code{\link[terra:vect]{terra::vect()}} (e.g.
shapefile, geopackage), or an object accepted by \code{\link[terra:vect]{terra::vect()}}.
Defaults to \code{rawFH}.}

\item{fields}{Character vector of attribute names in \code{vector_file} used to
derive the fire-history coding (e.g. \code{c("SEASON", "FIRETYPE_NO")}).}

\item{combField_multiplier}{Integer multiplier used when combining fields
into a single code (e.g. \code{SEASON * combField_multiplier + FIRETYPE_NO}).
(Currently passed through to downstream helpers; not used directly here.)}

\item{background}{Background value for rasterization (typically 0).}

\item{overwrite}{Logical; whether to overwrite existing outputs (passed to
downstream helpers).}

\item{datatype}{Output raster datatype (e.g. \code{"INT2U"}). Passed to downstream
helpers.}

\item{compress}{Compression method for written rasters (e.g. \code{"LZW"}).}

\item{memfrac}{Fraction of available memory to allow for processing.
Passed to downstream helpers.}

\item{progress}{Logical; show progress messages/bars in downstream helpers.}

\item{OtherAndUnknown}{Integer code for "Other/Unknown" fire type handling in
downstream logic.}

\item{start.SEASON}{Integer; first season/year for analyses in
\code{add_fire_lft_lby_ysf()}.}

\item{end.SEASON}{Integer or \code{NA}; optional final season/year for analyses.}

\item{max_interval}{Integer; maximum interval argument forwarded to
\code{add_fire_lft_lby_ysf()}.}
}
\value{
A list containing: \itemize{
\item OutDF sf polygons dataframe containing all the fire history attributes
each row represents a unique sequence of fire events
\item TimeSpan integer vector sequence of SEASONS to in the analysis output
\item YSFNames character vector of names of TSF years in output, needed by downstream functions
\item LBYNames character vector of names of LBY years in output, needed by downstream functions
\item LFTNames character vector of names of LBY years in output, needed by downstream functions
\item FH_ID integer vector giving the cell values of a raster of same extent resolution and crs
as the input r_template, these values are the indeces and ID values of the rows in OutDF and
used in downstream processes}
}
\description{
High-level wrapper that runs an end-to-end fire history raster workflow using
a raster template and a polygon vector file. The function:
\enumerate{
\item reads the vector fire-history layer with \code{\link[terra:vect]{terra::vect()}}
\item generates a fire-history raster stack (via \code{make_fh_raster_stack()})
\item block-packs the raster stack to a binary representation
(via \code{process_blocks_to_bin()})
\item reconstructs a packed raster from the binary files
(via \code{write_raster_from_bin()})
\item assigns unique fire-history IDs, splits encoded integers into
season and fire-type matrices (via \code{split_integer_first_last()}),
then
\item creates a list of outputs for downstream processing in FAME
\code{add_fire_lft_lby_ysf()}.
}
}
\details{
This is a \strong{workflow wrapper} around several lower-level
FAMEFMR utilities. It expects that the internal helper
functions \code{make_fh_raster_stack()}, \code{process_blocks_to_bin()},
\code{write_raster_from_bin()}, \code{split_integer_first_last()}, and
\code{add_fire_lft_lby_ysf()} are available in the namespace.

\strong{Encoding assumption}: the reconstructed raster values are assumed to be
integers that encode season and fire type digits; the call
\code{split_integer_first_last(as.integer(um), 5, 4, 1)} is currently hard-coded.
}
\examples{
\dontrun{
library(terra)
# r_template <- rast("template.tif")
# vector_file <- "fire_history.gpkg"
res <- fhProcess_raster_first(
  r_template = r_template,
  vector_file = vector_file,
  fields = c("SEASON", "FIRETYPE_NO"),
  start.SEASON = 1980
)
}
}
\seealso{
\code{\link[terra:vect]{terra::vect()}}, \code{\link[terra:rast]{terra::rast()}}, \code{\link[terra:subst]{terra::subst()}}
}
